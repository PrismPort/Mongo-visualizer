# github ssh key
ssh-keygen -t ed25519 -C "<USERNAME>@<EMAIL-PROVIDER>"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub 
# copy output to: https://github.com/settings/keys

# test github ssh connection
ssh -T git@github.com

# clone repository
git clone git@github.com:PrismPort/Mongo-visualizer.git

# .env.local
echo NEXTAUTH_URL=http://localhost:3000 > ./Mongo-visualizer/frontend/.env.local
openssl rand -base64 32 | xargs -I '{}' echo 'NEXTAUTH_SECRET={}' >> ./Mongo-visualizer/frontend/.env.local

# .env
cat <<EOF > ./Mongo-visualizer/backend/.env
EXPRESS_PORT=4000
CLIENT_ORIGIN="http://localhost:3000"
DOCKER="true"
EOF

